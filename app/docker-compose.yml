version: "3.7"

####################### Services Definition #######################
services:
  server-development:
    image: server:development
    container_name: server
    build: .
    volumes:
      - "./:/usr/src/app"
